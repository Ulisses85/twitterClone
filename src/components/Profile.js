const React = require('react');

const Profile = React.createClass({
  render: function () {
    return (
      <div className = "box profile-box" >Profile
      </div>

    );
  }
});

module.exports = Profile;
